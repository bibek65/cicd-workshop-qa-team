version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - echo "Installing dependencies..."
      - cd frontend
      - npm install

  build:
    commands:
      - echo "Running all tests with coverage..."
      - npm test
